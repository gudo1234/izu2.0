/*let handler = async (m, { conn, args, usedPrefix, command }) => {

  conn.reply(m.chat, `${e} Comando deshabilitado por el desarrollador.`, m)
}

handler.command = ['qr', 'code']
handler.group = true
export default handler*/
